# CURRENT_STATE

> 当前真实快照；不追加过程日志。稳定目标见 PROJECT_BRIEF，执行政策见 AGENTS。

## Current

- Lifecycle: `Stabilization`
- Plan: `RPF-00 = Complete`；`RPF-01 = Complete`；`RPF-02 = Complete`；`RPF-03 = Complete`；`RPF-04 = Complete`；`RPF-05 = Complete`；`RPF-06 = Complete`；`RPF-07 = Complete`；`RPF-08 = Complete`；`RPF-09 = Complete`；`RPF-10 = Complete`；`RPF-11 = Complete`；`RPF-12 = Complete`：已建立真实 Agent `FAIL`、Platform/Environment `ERROR`、独立 `rpf-failure-case-v1` 复现/验证闭环、首条 `rpf-regression-v1` Historical Regression 及 known-bad/fixed Candidate focused rerun；RPF-07 建立三成员 versioned Evaluation Suite、Baseline/Candidate 独立 Evaluation 与 member-level Comparison；RPF-08 在同一 reviewed evidence snapshot 上建立独立 versioned Quality Policy、确定性 Hard/Soft/Review Gate、Baseline/Candidate Release Decision 与只读 Release Decision 控制面；RPF-09 通过 disposable Spring Boot/H2 Control Plane candidate 验证 canonical metadata、immutable artifact refs、同步 HTTP/JSON ingestion、幂等/冲突、事务回滚和重启恢复边界；RPF-10 通过真实 PostgreSQL 16.15、Spring Boot/PostgreSQL JDBC candidate、持久 volume、service/数据库重启、备份恢复、并发冲突及 scoped authentication/authority probe，完成当前 Spike 的证据闭环；RPF-11 将该合同收敛为独立正式 Java/Spring Control Plane、PostgreSQL canonical metadata、immutable artifact registry、authenticated ingest/read API、append-only decision history、正式 Python client 与 API-backed Web read model；RPF-12 已在真实 GitHub-hosted runner 上接入 fresh PostgreSQL/正式 Control Plane/runtime Evaluation、canonical Release Decision read-back、principal separation、脱敏 artifact 与 Job Summary。
- Plan extension: `RPF-13 = Complete`；`RPF-14 = Complete`；`RPF-15 = Complete`；`RPF-16 = Complete`；`RPF-17 = Complete`；`RPF-18 = Complete`；`RPF-19 = Complete`；`RPF-21 = Complete`；`RPF-22 = Complete`；`RPF-23 = Complete`；`RPF-25 = Complete`；RPF-13 以真实 PostgreSQL-backed durable coordination candidate 完成 TU-004/TU-005 的正式实现边界调查，RPF-14 将其落入正式 Control Plane、Python worker、Web execution read model 与 RPF-12 durable CI path，RPF-15 完成 Candidate A production-like persistence/replacement/restore/release-boundary investigation 与 Candidate B 结构化比较，RPF-16 接入第二真实 Incident Remediation Agent 并完成跨 Agent Reliability Contract、Failure Case/Regression、独立 Evaluation/Quality Gate 与正式 durable worker Candidate 证据，RPF-17 建立确定性 Failure Intelligence、结构族/精确去重、跨 Agent 复用、negative controls 与 fail-closed version bisect，RPF-18 建立 additive Statistical Sampling Plan/Evaluation/Comparison、Wilson 区间、显式 denominator、flaky/safety gate、RPF-17 Failure Intelligence linkage、受控多 trial corpus 与正式 durable trial worker/read-only Web investigation surface，RPF-19 收敛 versioned Golden Demo profile、幂等 seed/start/stop、API-backed evidence-first Overview 与 interview-grade evidence drilldowns；RPF-21 在现有实现基础上完成 Docker 恢复后的 fresh PostgreSQL formal probe、local canonical Release Gate、Docker integration tests 与真实 GitHub-hosted gate 收口；RPF-22 关闭 RPF20-F05 的 server-side eligible discovery starvation、F14 的 local artifact containment/single-read verified bytes 与 F18 的 immutable writer/verifier authority boundary；RPF-23 关闭 RPF20-F15 的 child credential/path authority 与 RPF20-F16 的 Golden Demo process identity/rollback/ownership lifecycle boundary；RPF-25 在不改变 canonical API、metadata、raw evidence 或 route 身份的前提下建立 `en-US`/`zh-CN` presentation-only locale、集中 terminology/formatter/dictionary、Golden Demo 与 legacy read-only surface 的双语展示、status raw+explanation、completeness/pseudo harness 与 API-unavailable/deep-link accessibility contract；未引入 broker、scheduler、production HA、Approval 或 release/deploy endpoint，也未执行真实 Production deploy/release。
- Branch: `main`。
- Plan status: `RPF-25 = Complete`；继承 RPF-24 的 Web canonical snapshot/selector、Overview/Statistical/Failure feature ownership 与 shared status/data-source/CSS 边界，本轮建立 `en-US`/`zh-CN` presentation-only i18n、locale persistence/switch、terminology/status contract、locale-aware formatters、legacy read-only compatibility copy、pseudo expansion 与 completeness tests。
- RPF-24 local verification（2026-09-17）：Web `npm test` 为 10 个测试文件/33 个测试，`npm run typecheck` 与 `npm run build` 通过；production build 仍有 Vite shared chunk warning（JS 1,346.97 kB，gzip 194.35 kB），没有功能性构建错误。runtime `unittest` 为 77/77，`runtime/verify-reviewed-artifacts.py`、Golden Demo verifier、正式 Control Plane Maven package、RPF-14/RPF-22 formal probe/evidence verifier、RPF-23 contracts/lifecycle verifier 与本地 canonical Release Gate 均 `PASS`；当前本地 gate run `local-ec54a1485ae2` 使用 fresh PostgreSQL `postgres:16-alpine`、正式 Control Plane 与 durable worker，Baseline=`BLOCKED`、Candidate=`ELIGIBLE`、canonical read-back/authority/secret-redaction 通过，未执行 release/deploy。fixture Web 已在 1440/1280 desktop viewport 验证 Overview、Run、Failure、Statistical Evaluation、Statistical Comparison、Release Decision 深链，缺失实体显示 `Entity not found`，统计 trial 显式显示 `20 total / 0 resolved / 20 unavailable / 0 invalid` 且不创建伪造 Run 链接；API 不可用时显示 canonical unavailable 且不回退 fixture。`web/src` 定向扫描未发现 credential、Bearer token 或 private reasoning 字面量；历史 reviewed bytes、`DECISIONS.md` 与既有用户未跟踪 `docs/reviews/` 未改动。真实设备未执行。
- RPF-25 local verification（2026-09-17）：Web `npm test` 为 11 个测试文件/40 个测试，`npm run typecheck` 与 `npm run build` 通过；production build 仍有 Vite shared chunk warning（JS 约 1,415.61 kB，gzip 212.55 kB），没有功能性构建错误。runtime `unittest` 为 77/77，`runtime/verify-reviewed-artifacts.py`、Golden Demo verifier、正式 Control Plane Maven package、RPF-16/17/18 evidence verifiers 与本地 canonical Release Gate 均 `PASS`；gate run `local-f3ec46de0352` 使用 fresh PostgreSQL/正式 Control Plane/durable worker，Baseline=`BLOCKED`、Candidate=`ELIGIBLE`、Recovery=`RECOVERED`、canonical read-back/authority/secret-redaction 通过，未执行 release/deploy。fixture Web 已验证 Overview、legacy Run、Failure、Statistical Evaluation、Statistical Comparison 与 Golden Demo 深链在中英切换后保持 route/entity/scroll；用户 locale 持久化、`document.lang`/title、ARIA switch state、1440/1280 desktop no-overflow、API unavailable 两种语言 fail-closed、raw status/ID/hash/raw JSON 边界与 `?pseudo=1` expansion harness 均通过；真实设备未执行。RPF-25 未改 backend/schema/Control Plane API、reviewed corpus 或 `docs/reviews/`，未调用 Provider。
- Product / published version: 无。
- Last verified: 2026-09-17；RPF-01 的 14 项 synthetic/local 合同测试、RPF-02 的 7 项 host 生命周期测试与 1 项真实 Docker provider 集成测试、runtime 的 77 项 Python 合同/集成测试、正式 Control Plane 的 6 项 Java 单测与真实 PostgreSQL probe、Web 的 40 项 frontend tests（含 adapter/read-model tests）、frontend typecheck/build 与 reviewed artifact verifier 均通过；RPF-07 reviewed corpus 绑定历史 source `c607e5e38015c99fedcbba3efbcdc20834953f2fec7543313ead3ce0211d3adf`，RPF-08 reviewed Policy/Gate/Decision 保留历史 source `d6313e5849b2662d9badafa7be25d568e84ab6cc4912c9587495a5f9427bfd69`，verifier 同时接受该固定历史身份与当前 runtime source `3a1083279be0e6969c7222e86087647058e5a15562c1f99eccdc4297a21bd689`，Baseline 三成员质量 `0/3`、Candidate 三成员质量 `3/3`，两侧 required evidence coverage 均为 `3/3`；Historical Regression 为 `FAIL → PASS`，response-lost 为 baseline 未到达 reconcile、Candidate 已触发/观察/恢复；reported token 与 derived cost 均明确 `UNKNOWN`，未把缺失值当零。RPF-03/RPF-04 的真实 DeepSeek + Docker live Run、RPF-05 的 Agent FAIL source/reproduction 与 controlled Environment ERROR、RPF-06 的 stability/focused Run 均保留并通过验证；RPF-07 member Runs 使用真实 Docker Fresh-per-member deterministic profiles，RPF-08 Gate 复用脱敏 deterministic reviewed corpus，均未调用 Provider。历史 RPF-07/RPF-08 Evaluation/Comparison/Release Decision index/detail 的桌面 Chromium 1440/1280 viewport 证据仍有效；本 RPF-11 API-backed path 已在可用 CUA 的 in-app Chromium 代表性 305×612 视口验证稳定 deep-link、Timeline、State Diff、Evidence Inspector、artifact detail 与 API-unavailable fail-closed，精确 1440/1280 视口在当前 CUA surface 未执行。RPF-09 Spring Boot 3.4.5/H2 2.3.232 跨进程 persistence probe 通过 health、三类 ingestion/query、stable ref、duplicate/conflict、rollback、restart、missing/corrupt/unknown-schema 与 transport failure checks；真实设备未执行；历史与当前 reviewed JSON 的源码身份、状态、secret/private-protocol 边界离线核验通过。RPF-14 真实 probe 的 26 项检查与离线 evidence verifier 均 `PASS`，source identity 为 `810bf15c8ec6a34c7961ab8333fca7378534416675baf0c6eaf08ff3b3a43412`；真实 PostgreSQL 16.15、Java 17、正式独立 worker process、RPF-14 durable schema、cross-process response-lost reconcile、artifact ingest recovery、cancel/timeout、service/DB restart、custom-format backup/restore、schema mismatch、read API/metrics/retention 与 worker/CI/Agent/decision authority 均通过，probe 结束无 `rpf14*` container/volume/JVM 遗留。Web execution adapter/read-only routes 已通过 16 项 adapter/read-model tests、typecheck 与 production build；本地等价 RPF-12 durable Release Gate 已通过，Baseline/Candidate 两个 Job 均 `COMPLETED`，canonical Decision read-back 为 `BLOCKED` / `ELIGIBLE`，无 silent fallback、release 或 deploy。RPF-15 最终 production-like probe 的 21 项检查与离线 verifier 均 `PASS`，source identity 为 `2cbcbc7934658009a22808c8b9e965f5395c362601a409878905c96152523f64`，Product Release Identity 绑定 commit `2d0c7392e5d3500081c1bca029d95af24fa85a1a`、build `rpf-build-97f5b9bba7eaf759`，版本推荐 `0.1.0-rc.1`，状态明确为 `PRODUCTION_LIKE_PROBE_ONLY`；Candidate A 的 Web/Control Plane/Worker replacement、PostgreSQL named-volume replacement、custom-format dump/独立 restore、artifact 独立备份恢复、active-job reclaim/reconcile（2 attempts、effect count 1）、migration/application rollback、secret rotation、readiness/authority boundary 均通过并完成 cleanup；Candidate B 仅完成结构化比较，未执行 provider probe。RPF-17 通过 RPF-17 verifier、Maven package、Web typecheck/build 与本地 formal Control Plane probe；RPF-18 通过 77 项 runtime tests、40 项 Web tests、reviewed corpus verifier 与正式 PostgreSQL durable-trial probe；RPF-25 额外通过 i18n dictionary/legacy/pseudo checks 与 fixture/API-unavailable 双 locale 浏览器验收，真实设备未执行。
- RPF-19 local verification: Golden Demo profile verifier `PASS`（15 reviewed refs、2 explicit Agents、current runtime source binding、no Provider/cloud/release/deploy）；first start measured `21.56s`，Control Plane restart measured `13.93s`，`seed_demo --repeat 2` produced `172 IDEMPOTENT_REPLAY` and read back 15 refs；API-backed Overview 与 8 个关键 deep links smoke `PASS`，1440/1280 desktop viewport 无 horizontal overflow；Control Plane unavailable 时 Overview fail closed，恢复后正常 read-back；`mvn test package`、runtime 66 tests、RPF-16/17/18 verifiers、Web 24 tests/typecheck/build 均通过。Vite 保留 shared bundle warning（`index-BJ8WS9iu.js` 1,338.82 kB，gzip 192.26 kB），已在 `docs/DEMO.md` 记录为后续低风险 lazy-split 项，不阻塞本轮；真实设备验证未执行。
- RPF-23 local verification（2026-09-16）：`demo/verify-lifecycle.ps1 -Mode contracts` 与 fresh `-Mode lifecycle` 均 `PASS`（分别为 `.local/rpf23-71ebec6f`、`.local/rpf23-b3308f1c`）；Web child 仅有 read token，Worker 仅有 worker token/显式 IO root，Seed 仅有 seed credential，父 shell sensitive env 未被改变；state schema 为 `rpf-23-demo-runtime-state-v2`，child identity 绑定 PID、start time、executable、command fingerprint、session marker、expected port；foreign port/process/container、PID reuse、control-plane/seed/web/state-write rollback、restart、verified stop/RemoveData、Golden Demo verifier 与 state/log secret scan 均通过，probe 结束无 `rpf23` container/volume/process 遗留。worker path containment 新增测试包含在 runtime `77/77` 中；RPF-14/RPF-22 formal PostgreSQL probe 与 evidence verifier、Java Maven package、Web `24/24`、typecheck/build、Golden/RPF-16/17/18 verifiers 与 local canonical Release Gate 均 `PASS`。现有无 label legacy RPF-19 container/volume 已安全停止；`-RemoveData` fail closed，数据保留。该证据仅代表当前 Windows 本机 lifecycle hardening，不宣称 Production isolation/HA。

- RPF-14 remote delivery：真实 GitHub Actions [run `34757668703`](https://github.com/MyKr-YSteinsK/runproof/actions/runs/34757668703) / job `103724762724` 在提交 `33d5707` 上以 `Fresh canonical Release Gate` 成功结束（hosted runner，约 59 秒），并上传脱敏 gate artifact；Job Summary contract 验证通过，未执行 release/deploy。
- RPF-15 remote delivery：实施 commit `0f15fc6` 与 probe 稳定性修复 commit `2d0c739` 均已普通 push 到 `origin/main`；针对 source commit `0f15fc6` 的 RPF-12 hosted-runner [run `34802598786`](https://github.com/MyKr-YSteinsK/runproof/actions/runs/34802598786) / [job `103848110099`](https://github.com/MyKr-YSteinsK/runproof/actions/runs/34802598786/job/103848110099) 的 `Fresh canonical Release Gate` 已 `succeeded`（约 56 秒），`2d0c739` 则由本地干净工作树 RPF-15 probe/verifier 覆盖；该 hosted workflow 仅执行既有 canonical Release Gate，不执行 RPF-15 probe、Production deploy 或 release。
- RPF-16 本地交付证据：runtime source identity 为 `4569575d4a34853e5c85681dbac6576a8bf3a1252fc4cc34e8691c2378e8f494`；`python spikes/rpf-16/probe.py --verify` 与 `python spikes/rpf-16/verify-evidence.py` 均 `PASS`。两个显式 Agent contract（Production Change / Incident Remediation）共享窄 integration shape；Incident local/external/response-lost 三类 case、known-bad harmful external remediation、Failure Case fresh reproduction、2 条 stability、Regression promotion/focused result、3-member independent Suite、Baseline/Candidate Evaluation、Comparison、Quality Policy/Gate/Decision 均通过。Baseline Agent Quality `0/3`、Candidate `3/3`，required evidence coverage 均 `3/3`，Comparison 为 `CANDIDATE_IMPROVED`，Gate/Decision 为 `BLOCKED` → `ELIGIBLE`；Candidate Decision 仍为 `DECISION_ONLY`，release/deploy 均未执行。
- RPF-16 formal durable evidence：`python spikes/rpf-16/probe.py --run` 在真实 PostgreSQL 16.15 + 正式 `control-plane/` + 独立 `runtime.runproof_runtime.durable_worker` 上通过一次 Incident Candidate Evaluation；mechanism 为 `POSTGRESQL_POLL_CLAIM_LEASE`，job `job-rpf16-formal-candidate`，attempt `1`，terminal evidence `execution-evidence-job-rpf16-formal-candidate`，worker exit `0`，canonical job `COMPLETED`，stdout/stderr secret scan safe，临时 `rpf16-*` resources 已 cleanup。该证据不等于 Production HA、真实服务 remediation 或 release/deploy。
- RPF-16 remote delivery：实施 commit `8dfd5e1f90de3326cf8eca132487136570b9c703` 已普通 push 到 `origin/main`；RPF-12 hosted-runner [run `34810366065`](https://github.com/MyKr-YSteinsK/runproof/actions/runs/34810366065) / [job `103870322326`](https://github.com/MyKr-YSteinsK/runproof/actions/runs/34810366065/job/103870322326) 的 `Fresh canonical Release Gate` 已 `success`（约 1m03s），包含 RPF-16 corpus verifier repository contract check，并生成脱敏 `rpf-12-release-gate-34810366065-1` artifact；该 workflow 未执行 release/deploy。
- RPF-16 final verification delta：runtime test suite 为 `52/52`，Web adapter/read-model tests 为 `17/17`，并额外通过 RPF-16 corpus verifier、formal durable-worker verifier、Maven package、Web typecheck/build；该增量与上方历史 RPF-01～RPF-15 汇总保持分层记录。
- RPF-17 local delivery：runtime source identity 为 `9654c309b427de8da42192f2cda79383c257a7680b46ce4ca2098feafc19dad6`；`python spikes/rpf-17/probe.py --verify` 与 `python spikes/rpf-17/verify-evidence.py` 均 `PASS`。reviewed corpus 包含 5 份 `rpf-failure-intelligence-v1`、3 份 `rpf-failure-cluster-v1` 与 1 份 `rpf-version-bisect-v1`；Production/Incident exact signature 保持历史值，两个 exact groups 各 4 次 occurrence，共享 cross-Agent structural family 但 domain family 不合并，Provider/Environment/Invalid Input negative controls 均为 `NOT_AGENT_FAILURE`。Incident controlled version line 为 fixed `PASS` → known-bad `FAIL`，first bad 为 `incident-remediation-agent-v1-known-bad`；non-monotonic/error/compatibility 结果 fail closed，recommendation 不自动 promotion。
- RPF-17 Control Plane/Web delivery：正式 `control-plane/` 使用既有 PostgreSQL canonical metadata + immutable artifact store 读写 9 个新 artifact（Failure Intelligence `5`、Failure Cluster `3`、Version Bisect `1`），`python spikes/rpf-17/probe.py --run` 证明 artifact read-back、derived write 不改变 canonical source 与 no release/deploy；Web 新增 `/failure-intelligence`、`/failure-intelligence/clusters/:clusterId`、`/version-bisects/:bisectId` 与既有 Failure Case detail 派生分析面，继续只读、fail closed。RPF-17 没有新增数据库表、Provider/LLM/embedding/ML、自动 promotion、第三 Agent 或真实 Production remediation。
- RPF-17 final verification delta：runtime test suite `60/60`、Web adapter/read-model tests `18/18`、RPF-16 historical compatibility verifier、RPF-17 corpus verifier、formal Control Plane probe、Maven package、Web typecheck 与 production build 均 `PASS`；RPF-16 reviewed artifacts 继续保留冻结 source identity `4569575d4a34853e5c85681dbac6576a8bf3a1252fc4cc34e8691c2378e8f494`，未被新 runtime module 静默重写；fixture in-app Chromium 已验证 Failure Intelligence index、cross-Agent Cluster detail、Version Bisect detail 与 Failure Case 派生分析 panel；真实设备未执行，未将其写成自动化 PASS。
- RPF-17 remote delivery：实现 commit `f9d11aa` 与 RPF-16 historical compatibility fix `be73017` 均已普通 push 到 `origin/main`；修复后的真实 GitHub-hosted runner [run `34847854403`](https://github.com/MyKr-YSteinsK/runproof/actions/runs/34847854403) / [job `103988065991`](https://github.com/MyKr-YSteinsK/runproof/actions/runs/34847854403/job/103988065991) 在 `be73017` 上以 `Fresh canonical Release Gate` 成功结束（约 77 秒）。Repository contract checks、RPF-16 historical compatibility、RPF-17 corpus verifier、Job Summary contract 与 gate artifact upload 均通过，生成脱敏 artifact `rpf-12-release-gate-34847854403-1`（artifact id `10348812143`）；该 workflow 未执行 release/deploy。
- RPF-18 remote delivery：实现 commit `c23ad75` 已普通 push 到 `origin/main`；真实 GitHub-hosted runner [run `34942385897`](https://github.com/MyKr-YSteinsK/runproof/actions/runs/34942385897) / [job `104293821904`](https://github.com/MyKr-YSteinsK/runproof/actions/runs/34942385897/job/104293821904) 的 `Fresh canonical Release Gate` 已 `success`（约 1m34s），生成脱敏 artifact `rpf-12-release-gate-34942385897-1`（artifact id `10385294868`）；该 workflow 未执行 release/deploy。
- RPF-19 remote delivery：实现 commit `f647472` 已普通 push 到 `origin/main`；真实 GitHub-hosted runner [run `34947014281`](https://github.com/MyKr-YSteinsK/runproof/actions/runs/34947014281) / [job `104308723805`](https://github.com/MyKr-YSteinsK/runproof/actions/runs/34947014281/job/104308723805) 的 `Fresh canonical Release Gate` 已 `success`（约 1m13s），repository contract checks 已包含 Golden Demo profile verifier，生成脱敏 artifact `rpf-12-release-gate-34947014281-1`（artifact digest `sha256:2a16d7ca4fd3fd54112684fea7dba02d1f76b4a83543bbe99d302a64720ecf0f`）；GitHub 报告 Node 20 forced-to-24 与 `setup-java@v4` 弃用 warning，但未影响 gate；该 workflow 未执行 release/deploy。
- RPF-22（2026-09-16）状态为 `Complete`：修复 durable worker 的 server-side eligible discovery，新增 `eligible=true` bounded query 与 PostgreSQL partial discovery index，terminal history 不再占据 worker discovery page；正式 probe 以 24 条 `FAILED_PLATFORM` 历史记录加后置 queued job 验证 worker 完成 queued job，并以过期 active lease 验证 eligible reclaim。`LocalFileArtifactStore` 现在逐级检查 canonical root、绝对路径、traversal、junction/reparse parent containment，verified artifact 使用同一份读取/校验 bytes 返回 document，Controlled mutation hook 不会触发第二次普通路径读取；runtime candidate writer 改为 immutable replay/conflict，canonical `verified` 语义仍由 Control Plane registry/verifier 负责。正式 probe 结果为 `.local/rpf-14/control-plane-30c6129011/probe-result.json`，RPF-14/RPF-22 evidence verifier、Docker PostgreSQL 16、worker、artifact negative matrix、Maven、runtime `75/75`、Web `24/24`、typecheck/build 与 local canonical Release Gate 均 `PASS`；junction escape/absolute path 均 HTTP `422`，formal result 无 credential/private protocol，temporary `rpf14` resources 已 cleanup。历史 RPF-01～RPF-21 reviewed bytes 未刷新；未引入 RPF-23、broker、scheduler、capacity、object storage、OAuth/OIDC/SSO 或 release/deploy。
- RPF-21（2026-09-16）状态为 `Complete`：在 `ab811b0` 的统计门禁/终端 evidence 修复上补齐 malformed evidence manifest 的 fail-closed 回归，避免未知 schema 因 Java `Set.of(...).contains(null)` 变成 500；修复与 Java negative contract test 已在 commit `8be22d6af8cf6bd3e20e8d2f86a2408ea727ee83` 普通 push 到 `origin/main`。Docker Desktop `docker-desktop` engine/client/server `29.7.2` 恢复后，fresh `python control-plane/probe.py` 以 PostgreSQL `postgres:16-alpine` 完成 formal Control Plane/worker/terminal evidence/current-attempt fencing/UNKNOWN_OUTCOME reconcile/authority/backup-restore/schema mismatch/read API 验证并 `PASS`，结果为 `.local/rpf-14/control-plane-6b49e3288d/probe-result.json`，formal source SHA `3f45ba96c3211457666ba87d34e69ae10b3accc7d8929274432ffb6bd93db389`，`rpf14` container/volume cleanup 为 0；提交后的 `python ci/run_release_gate.py` 重新 `PASS`，source commit 为 `8be22d6`，Baseline=`BLOCKED`、Candidate=`ELIGIBLE`、Recovery=`RECOVERED`、registered fresh evidence `16`、无 silent fallback/release/deploy。完整 runtime suite 为 `73/73`，此前被 Docker 主机跳过的 `test_fresh_environment_gates_mutation_and_cleanup` 与 `test_initial_mismatch_stops_before_agent` 均实际执行并通过；RPF-18 verifier、reviewed-artifact verifier、RPF-16/17 verifier、RPF-19 Golden Demo verifier 均通过，历史 reviewed bytes 未静默改写。真实 GitHub-hosted [RPF-12 run #17](https://github.com/MyKr-YSteinsK/runproof/actions/runs/35071819220) 在 `8be22d6` 上 `success`（约 1m18s），生成脱敏 gate artifact，未执行 release/deploy；未执行 factory reset，未触碰 `.local/rpf-20`、历史 reviewed corpus 或 Docker 数据卷。
- RPF-10 真实验证：Docker Desktop `desktop-linux` 上使用 PostgreSQL 16.15、JDBC 42.7.5 与 Java 17.0.12 完成 schema/migration、Run/Evaluation/Release Decision 写读、幂等/冲突/回滚、并发 race、Control Plane/数据库重启、storage failure、schema mismatch、`pg_dump`/独立 `pg_restore` 及四类 principal 负向权限测试；固定脱敏证据 source hash 为 `4c97ff0bd88ca14e2a30646e03531b3509c52a5c9e8b7e05c85ad6dc34f8a02a`，probe 与离线 verifier 均为 `PASS`，凭据/Authorization/private protocol 未进入结果、日志或 artifact。
- RPF-11 真实验证：正式 Control Plane 使用 `rpf-11-postgresql-canonical-schema-v1`、Java 17/Spring Boot 3.4.5、PostgreSQL 16.15/JDBC 42.7.5 与 local immutable artifact store；fresh PostgreSQL、service/database restart、rollback、same-fingerprint replay、identity/idempotency conflict、concurrent race、历史 corpus registration、Decision superseding、missing/corrupt/wrong-schema/wrong-identity/path-traversal/source-mismatch、read API、backup/restore、schema mismatch、auth authority matrix 与 secret redaction probe 均 `PASS`。正式 Web 默认 API-backed，服务不可用时 fail closed，不以 static fixture 静默回退。
- RPF-12 真实验证：workflow `.github/workflows/release-gate.yml`（`RPF-12 Canonical Release Gate`）由 push、pull_request、workflow_dispatch 触发；GitHub workflow ID `356937222` 已识别为 active。最终 implementation source `e4bdb00e952d5c5441bd96b6984033fdb5a54194` 的 push run `34742862720` 与独立 dispatch run `34742919966` 均 `success`，dispatch job `103685573138` 为 `success`；fresh PostgreSQL/正式 `control-plane/` readiness 通过，formal `runtime/` 使用 fresh-per-member Evaluation。Baseline Evaluation `evaluation-ci-34742919966-1-baseline` / Decision `release-decision-ci-34742919966-1-baseline` 为 `BLOCKED`，Candidate Evaluation `evaluation-ci-34742919966-1-candidate` / Decision `release-decision-ci-34742919966-1-candidate` 为 `ELIGIBLE`，canonical API read-back 与 CI final source 均一致；coverage `3/3`，Historical Regression `PASS`，Recovery/Fault `RECOVERED`，release/deploy 均未执行。
- RPF-12 交付证据：artifact `rpf-12-release-gate-34742919966-1`（ID `10312746964`）包含 JSON result 与脱敏 Summary mirror；Job Summary contract step `success`，Summary 包含 Baseline control、Candidate Gate、Regression、Recovery/Fault、coverage、Final Decision 与 no-release 声明。`ci-service`、`evidence-ingest-service`、`agent-runtime` 对 Decision 写入均为 HTTP 403，独立 `decision-writer-service` 登记成功并 replay 为 `IDEMPOTENT_REPLAY`；Decision immutable history 均为 true。最终 run/artifact/summary 日志扫描未发现 DeepSeek key、DB password、Authorization header、Bearer credential 或 private reasoning。
- RPF-13 真实验证：disposable Spring Boot 3.4.5/JDBC candidate 使用真实 Docker PostgreSQL 16.15；job/attempt/operation/evidence schema、durable submit/replay、两 worker concurrent claim、heartbeat/version fencing、lease expiry safe reclaim、stale finalize、pre-send `NOT_SUBMITTED`、response-lost 子进程 → `UNKNOWN_OUTCOME` → environment receipt reconcile、artifact ingest replay、queued/running cancellation、timeout `INCONCLUSIVE`、PostgreSQL 与 candidate JVM restart、future CI submit/poll/read、operator read model、forbidden private-payload rejection 与 secret/private-protocol boundary 均 `PASS`。固定脱敏 evidence source hash 为 `5a54a606b7611f0a1a3ced06fa5abe52f7b29b0b7c9ae7acb202aeaea474aed9`；`python spikes/rpf-13/verify-evidence.py` 通过，rpf13 container/volume 均清理。

## Major capabilities and technical shape

- 已有 [RPF-01 probe 与结论](../../spikes/rpf-01/README.md)：Node.js 24 无第三方依赖的 disposable 本地状态实验，不是正式 Agent/runtime/environment。
- 已有 [RPF-02 probe 与结论](../../spikes/rpf-02/README.md)：Docker Desktop `desktop-linux` / Engine `29.6.1` 上，Fresh-per-run 的 writable-layer、重建无污染、双实例隔离、named-volume persistent-state 对照、cleanup/quarantine 与 environment identity/provenance 合同均有实际证据；Reuse-and-reset 仍是未来优化候选。
- 已有 [RPF-03 product runtime 与 vertical slice](../../runtime/README.md)：Python formal runtime 绑定 `production-change-release@1.0.0`，以 DeepSeek `deepseek-flash` non-thinking tool loop 驱动受控 Docker 状态变更；正常 live 与 response-lost live 都由实际状态、receipt、S0→S1 diff 和 deterministic verifier 判定 `PASS`，各路径仅一次 mutation、无 blind retry，响应丢失先 reconcile。
- 已有 [RPF-04 Evidence Control Plane](../../web/README.md)：React + TypeScript + Vite 的正式 Desktop Web read-only surface，通过薄 data adapter 消费真实 v2 reviewed artifacts；提供 Run entry、context identity、stable timeline/event deep-link、Fault ≠ Failure recovery path、S0→S1 State Diff、Evidence/Invariant Inspector 与 raw payload 渐进展开。历史 v1 与当前 v2 明确分层，未静默迁移。
- 已有 RPF-05 Failure Investigation surface：`/runs`、`/runs/:runId`、`/failures`、`/failures/:failureCaseId` 均消费本地 reviewed corpus；Agent FAIL 显示 violated invariant、guard evidence、state protection 与 Failure Case 引用，Platform/Environment ERROR 显示 pre-Agent readiness boundary、Agent 未启动与质量排除；Failure Case 只在 stable signature、invariant、evidence pattern、健康边界和 fresh identity 均匹配后进入 `validated`，明确不是 Regression。
- 已有 RPF-06 Regression workflow：`regression.py` 提供 reproducibility、relevance、stability、non-duplicate、explicit expectation 五项结构化 promotion gate；`rpf-regression-v1` 以稳定 signature-derived identity、versioned expected behavior/oracles、promotion decision、evidence refs 和 focused rerun history 独立于 Run/Failure Case；`rpf-regression-result-v1` 分离 Run outcome 与 Regression result，最小 `Historical Regression` collection 通过静态 reviewed manifest 提供稳定 lookup；known-bad `1.0.0-known-bad-unsafe-precondition` 稳定 FAIL，fixed Candidate `1.0.1-observe-before-mutation-fix` 通过同一 Regression。
- 已有 RPF-07 Evaluation Suite / Comparison vertical slice：`evaluation.py` 提供 `rpf-evaluation-suite-v1`、`rpf-evaluation-result-v1`、`rpf-evaluation-comparison-v1`；固定 Suite `rpf-minimal-reliability-suite@1.0.0` 含 Normal / Functional、Recovery / Fault、Historical Regression 三个 required member。每个 member 在独立 Docker Fresh-per-member 环境中执行，Run outcome、item result、Agent quality、evidence coverage、Recovery/Regression 与 raw-vs-derived token/cost/latency 分开保存；Baseline `1.0.0-known-bad-unsafe-precondition` 与 Candidate `1.0.1-observe-before-mutation-fix` 共享 Suite contract 但拥有独立 Evaluation/Run/Environment identity。Comparison 输出每成员 `IMPROVED/REGRESSED/UNCHANGED/INCOMPARABLE` 与 aggregate delta，当前三成员均 `IMPROVED`；不输出 Quality Policy 或 Release Decision。
- 已有 RPF-08 Quality Policy / Release Gate vertical slice：`quality.py` 提供独立 `rpf-quality-policy-v1`、`rpf-quality-gate-evaluation-v1`、`rpf-release-decision-v1`；首个 Policy 兼容 `rpf-minimal-reliability-suite@1.0.0`，冻结 required evidence coverage、无 required Agent FAIL、Historical Regression PASS、Recovery PASS、valid evidence contract 与 identity compatibility 等 Hard requirements，usage/cost/latency unknown 为 Soft warning，并支持确定性 Review rule。固定 precedence 为 Hard blocker → evidence insufficient → review required → eligible；当前 Baseline Gate/Decision 为 `BLOCKED`，Candidate Gate/Decision 为 `ELIGIBLE`，所有 decision 都保留 blocking/review/soft evidence、stable refs、source identity、immutable history 和 `release_executed=false` / `deployment_authorized=false`。
- RPF-09 disposable Control Plane candidate：Spring Boot 3.4.5 + JDK 17、H2 2.3.232 file persistence 与本地 immutable artifact registry 完成 `rpf-09-metadata-schema-v1` migration、Run/Evaluation/Release Decision manifest ingest/query、stable ref 校验、duplicate/conflict、事务回滚、服务重启恢复、missing/corrupt/unknown-schema fail-closed 及同步 HTTP/JSON 跨进程 probe；不等同正式 PostgreSQL/API backend。
- RPF-10 disposable PostgreSQL/Auth boundary candidate：Spring Boot 3.4.5 + PostgreSQL JDBC 42.7.5 在真实 PostgreSQL 16.15 上完成 migration、canonical metadata insert/query、immutable artifact ref、idempotency/conflict、事务回滚、并发 race、Control Plane/PostgreSQL restart、schema mismatch、storage failure、pg_dump/pg_restore 与 service-to-service principal boundary probe；不等同正式后端、生产 HA 或 durable execution。
- RPF-11 formal Control Plane：`control-plane/` 为独立 Spring Boot 产品模块，PostgreSQL 保存精简 canonical metadata（identity/version/status/summary/refs/artifact pointer/audit facts），local store 只保存经过 schema/kind/entity/source/hash 验证的 immutable artifact；`/api/v1` 提供 health/ready、completed evidence 与 trusted Decision ingest、Runs/Failures/Regressions/Evaluations/Comparisons/Release Decisions read API、artifact resolution、audit/boundary；正式 Python client 负责 manifest、认证、幂等 replay/reconcile、typed error 与 corpus registration，Runtime 不直写 DB。
- RPF-13 durable execution/job transport spike：`spikes/rpf-13/control-plane/` 使用 Spring Boot 3.4.5/JDBC 与 PostgreSQL 16.15 candidate，分离 mutable `rpf_execution_job`、append-only attempts/events、stable operation/effect receipt 与 immutable execution evidence；`POST /api/v1/jobs` submit/replay，poll/claim/heartbeat/start，operation prepare/apply/confirm/unknown/reconcile，evidence ingest、complete/fail-platform、cancel/timeout 与 job read model 均通过真实 probe。`FOR UPDATE` claim race 只保留一个 owner；lease expiry 仅在无 uncertain operation 时 safe reclaim，否则 `RECONCILE_REQUIRED`；跨 worker process response-lost 先 reconcile，effect count 保持 1。该 candidate 是 PostgreSQL poll/claim recommendation，不是正式 backend、production durable worker、broker、scheduler、HA 或 release/deploy authority。
- RPF-14 formal Durable Execution：正式 `control-plane/` 使用 additive `rpf-14-durable-execution-schema-v1`，PostgreSQL 保存 mutable Job coordination projection、Attempt/Operation/Event history 与 immutable execution evidence refs；`DurableExecutionController` 提供 submit/replay/conflict、list/detail/metrics、claim/start/heartbeat/reclaim/stale fencing、operation prepare/dispatch/NOT_SUBMITTED/UNKNOWN_OUTCOME/reconcile、evidence ingest、complete/fail-platform、cancel/timeout。正式 `runtime.runproof_runtime.durable_worker` 只通过 Python HTTP/JSON client poll/claim/heartbeat/执行 deterministic Evaluation、写入 canonical artifacts/evidence 并 terminalize；CI Baseline/Candidate 使用同一 worker mechanism，durable 失败不回退 direct path。Web 新增 `/executions` 与 `/executions/:jobId` 只读调查面，保留 state/outcome、attempt/lease/operation/reconcile/evidence/event/raw JSON 分层。
- RPF-16 cross-Agent Reliability Contract：`runtime/runproof_runtime/agent_contract.py` 仅注册两份 reviewed adapter，显式记录 Agent identity/domain/type、Scenario、Tool、Environment、Verifier、execution profile 与 evidence schema compatibility；`incident.py` 提供 Incident Remediation Agent 的 service/dependency/change evidence、bounded remediation、safe stop、response-lost reconcile、recovery verifier 与 source-bound `rpf-run-evidence-v2`。`failure_case.py`、`regression.py`、`evaluation.py`、`quality.py` 与 durable worker 通过 registry dispatch Incident profile，不复制 Production Change 语义或开放动态加载。
- RPF-16 Web surface：`/agents` 与 `/agents/:agentId` 展示两份 Agent identity/domain/type、contract、version/config、Scenario/Tool/Environment/Verifier refs、linked Runs/Failures/Regressions/Evaluations/Decisions，以及 Incident first divergence、dependency facts、side-effect/effect count 和 Facts/Verified/Inference 分层；既有调查页同时显示 Agent identity/domain。页面继续只读、不发起 Agent/mutation/rerun/promote/evaluate/compare/release。
- RPF-18 Statistical Evaluation：`runtime/runproof_runtime/statistical.py` 固化 `rpf-statistical-sampling-plan-v1`、`rpf-statistical-evaluation-v1`、`rpf-statistical-comparison-v1` 与独立 Policy/Gate/Decision；Sampling Plan 冻结 fresh-per-trial、attempt budget、Agent/config、Scenario、controlled sequence、95% Wilson method/version 与 metrics，Evaluation 分离 `AGENT_PASS + AGENT_FAIL` quality denominator 与 attempted/evidence denominator，保留 Platform/Environment/Invalid/Inconclusive/Cancelled exclusion，Flaky/Safety/Failure Intelligence linkage 和 interval-aware Comparison。`spikes/rpf-18/probe.py` 的受控 corpus 包含 Stable Good、Baseline、Flaky Reliability、High Pass + Safety Flake、Evidence-poor/Platform noisy 五组；reviewed corpus 为 20 份统计 artifact，决策为 Stable `ELIGIBLE`、Flaky `REVIEW_REQUIRED`、Safety `BLOCKED`、Evidence-poor `INCONCLUSIVE`，Baseline 仅作为 comparison source。正式多 trial 路径复用既有 PostgreSQL durable worker，每个 Trial 独立 Job/Run/environment，replay 不增加样本计数；Web 提供 `/statistical-evaluations`、`/statistical-evaluations/:evaluationId`、`/statistical-comparisons` 与 detail，只读且 decision-only。
- RPF-19 Golden Demo：`demo/rpf-19-golden-demo-v1.json` 固化 demo identity/version、source/runtime hash、两份 Agent identity、Baseline/Candidate、四类 Statistical Evaluation、Failure Cluster/Case、Version Bisect、Release Decision、UNKNOWN_OUTCOME reconcile run 与 15 个 reviewed artifact refs；`demo/verify-golden-demo.py` 做 source-bound immutable corpus/secret/mock/prod marker 校验，`demo/seed_demo.py` 只经正式 Control Plane HTTP/JSON registry 幂等注册并 canonical read-back；`demo/start-demo.ps1` / `stop-demo.ps1` 提供单机 PostgreSQL named volume、Control Plane、reviewed seed、API-backed Web 的可重启路径。`/` 与 `/overview` 是 evidence-first read-only investigation surface，覆盖 Candidate→Evidence→Failure→Regression→Gate、统计、Failure Intelligence、durable reconcile 与 claim/evidence/boundary；不发起 live Agent、Provider、rerun、release 或 deploy。
- RPF-01 的真实 `deepseek-flash` non-thinking 正常路径及 response-lost/reconcile、thinking 单个 user turn 内多轮 Tool Calls、JSON Output 与安全 HTTP 400 样本通过；各状态路径仅发生一次 mutation，无 blind retry。
- RPF-01 最终脱敏样本 15 请求（14 个 200、1 个 400），10,123 已报告 tokens，派生成本 0.00574568 CNY；400 无 usage，成本 unknown。模型返回 alias/fingerprint，没有不可变版本号；文档声明 V4.1-Flash 与响应事实分开保存。
- 正式产品 runtime / Provider adapter 已有 RPF-03～RPF-08 的单 Run Prototype 实现，RPF-11 已建立正式 Java Control Plane、API/backend service、PostgreSQL canonical DB 与 Python client，RPF-14 已建立正式 durable worker 与 Job Transport；Desktop Web 通过 DTO→view-model adapter 默认消费正式 API，static reviewed corpus 仅保留为显式 fixture/test source；RPF-09/RPF-10/RPF-13 仍作为历史 disposable probes 保留；CLI 提供 check/promote/focused/verify Regression、build/validate/evaluate/compare/verify Evaluation、validate/evaluate/create/verify Quality Gate/Release Decision 与 durable worker 入口；broker、scheduler、production HA、Approval、deploy/release endpoint 与 credential/私有 continuation 持久化仍无。
- CI：GitHub Actions `RPF-12 Canonical Release Gate` 已接入并以 canonical Release Decision read-back 作最终 gate；Baseline/Candidate Evaluation 已迁移为正式 durable submit/poll/read；RPF-14 delivery commit `33d5707` 的 hosted-runner run `34757668703` / job `103724762724` 已成功；deployment workflow / endpoint / release：无。

## Workflow / delivery facts

- 用户已确认 GitHub identity：`MyKr-YSteinsK/runproof`，`Public`。
- Git Credential Manager 与 GitHub API 已验证账户 `MyKr-YSteinsK`；无需依赖 gh CLI。
- Remote: `origin` → https://github.com/MyKr-YSteinsK/runproof.git；GitHub 已验证为 Public，默认分支 `main`。
- Upstream: `main` → `origin/main`；RPF-04 delivery record commit `d03cb59`、RPF-05 focused commit `408506a`、RPF-06 implementation commit `1ee20b5` 与 RPF-07 implementation commit `bfd49c9` 均已普通 push，RPF-06 delivery-state update 亦随后提交并 push；RPF-07 implementation 包含 reviewed Suite/Evaluations/Comparison corpus、runtime/Web surface 与当前合同文档，CLI 路径说明 follow-up commit `ff7cb0a` 亦已 push；RPF-08 Quality Policy/Release Gate/Decision implementation commit `6a153e1` 与 RPF-09 Control Plane persistence/API spike commit `a4cc895` 已普通 push；RPF-10 PostgreSQL/Auth spike implementation、probe、reviewed evidence 与项目状态已由 commit `0a65a7b` 固化；RPF-11 formal Control Plane/PostgreSQL/API/client/Web implementation commit `c76d3ce` 已建立；RPF-12 implementation/fix commits `1c7c2ef`、`9d4da04`、`ed02efc`、`ef13d08`、`e4bdb00` 已由 normal fast-forward push 交付，未使用 force push、rebase 或 history rewrite。
- Upstream: `main` → `origin/main`；RPF-04 delivery record commit `d03cb59`、RPF-05 focused commit `408506a`、RPF-06 implementation commit `1ee20b5` 与 RPF-07 implementation commit `bfd49c9` 均已普通 push，RPF-06 delivery-state update 亦随后提交并 push；RPF-07 implementation 包含 reviewed Suite/Evaluations/Comparison corpus、runtime/Web surface 与当前合同文档，CLI 路径说明 follow-up commit `ff7cb0a` 亦已 push；RPF-08 Quality Policy/Release Gate/Decision implementation commit `6a153e1` 与 RPF-09 Control Plane persistence/API spike commit `a4cc895` 已普通 push；RPF-10 PostgreSQL/Auth spike implementation、probe、reviewed evidence 与项目状态已由 commit `0a65a7b` 固化；RPF-11 formal Control Plane/PostgreSQL/API/client/Web implementation commit `c76d3ce` 已建立；RPF-12 implementation/fix commits `1c7c2ef`、`9d4da04`、`ed02efc`、`ef13d08`、`e4bdb00` 已由 normal fast-forward push 交付；RPF-15 implementation commit `0f15fc6` 与 probe stability fix `2d0c739` 已普通 push，未使用 force push、rebase 或 history rewrite。
- Delivery model: `push-only`；无部署触发。具体授权与执行规则见 AGENTS。
- RPF-22 remote delivery：实现与状态 commit `8ce64dabc1029b33364ee2bc202f137ef6a9cc1a` 已普通 push 到 `origin/main`；GitHub-hosted [RPF-12 run `35077060386`](https://github.com/MyKr-YSteinsK/runproof/actions/runs/35077060386) / [job `104731878699`](https://github.com/MyKr-YSteinsK/runproof/actions/runs/35077060386/job/104731878699) 在该提交上以 `Fresh canonical Release Gate` 成功结束，repository contract checks、fresh canonical gate、Job Summary contract 与脱敏 artifact upload 均通过；未执行 release/deploy。
- RPF-23 remote delivery：实现 commit `9000c6e9f928300b490e8ca5a829afa6b209452b` 已普通 push 到 `origin/main`；GitHub-hosted [RPF-12 run `35104299919`](https://github.com/MyKr-YSteinsK/runproof/actions/runs/35104299919) / [job `104821379233`](https://github.com/MyKr-YSteinsK/runproof/actions/runs/35104299919/job/104821379233) 在该提交上以 `Fresh canonical Release Gate` 成功结束，耗时约 1m16s，生成脱敏 gate artifact；workflow 未执行 release/deploy。

- RPF-24 remote delivery：实现与状态提交 `fcaf0260514229d3ccee32644cef490623e6e5fe` 已普通 push 到 `origin/main`；真实 GitHub-hosted [RPF-12 run `35175091806`](https://github.com/MyKr-YSteinsK/runproof/actions/runs/35175091806) / [job `105054934450`](https://github.com/MyKr-YSteinsK/runproof/actions/runs/35175091806/job/105054934450) 已 `success`（约 1m03s），生成脱敏 artifact `rpf-12-release-gate-35175091806-1`（digest `sha256:38d75ed15383154a5541d1c264ff81a6068ffb1dc271fb9fe5908ab25a1bab58`）；workflow 未执行 release/deploy，仅报告 Node.js 20 forced-to-24 与 `setup-java@v4` 弃用 warning。

## Known limitations / risks

- TU-001 最小 non-streaming/single-turn Tool-Using 合同获得实验支持，建议首个 Prototype 默认 non-thinking。thinking 的跨 user turn、fault/restart/continuation 恢复及 streaming 未验证；有限样本不证明长期稳定性。
- RPF-02 已取得 Prototype-level minimum Environment isolation/reset 合同证据，但不等于 production-grade isolation、HA 或 deployment readiness。首个 Prototype 建议采用 Fresh-per-run 语义；Reuse-and-reset 仍需 provider 级 snapshot/restore、ownership 与 quarantine 成本证据后再启用。TU-002 已在该范围关闭；RPF-14 已关闭 TU-004/TU-005 的最小正式 durable slice，但 production-grade retention/backup/object-store、容量、rollout、HA 与完整身份/审批边界仍待后续 Checkpoint；TU-003 Fault Injection Boundary、TU-006 Replay Semantics、TU-007 Non-deterministic Evaluation、TU-008 Agent Integration Contract、TU-009 Large Trace Visualization 仍未整体解决。
- RPF-03 只证明一个 Production Change Agent、一个版本化 Scenario、DeepSeek non-thinking/non-streaming 和两个 live Fault Profile 的单 Run Prototype 合同；不等于通用 Agent SDK、生产级 isolation/HA、durable resume、streaming/thinking continuation、真实生产 destructive operation 或发布授权。当前 Docker 是 evidence/provider 实现，不是永久产品身份。
- RPF-11 Web 已将 Runs、Evaluations、Comparisons、Release Decisions 以及 Failures/Regressions 切换为 Control Plane API-backed read-only adapter；Evaluation/Comparison 仍展示 Suite matrix、Run/item 分层、quality/coverage、Recovery/Regression、raw-vs-derived metrics 与 Baseline/Candidate diff，页面不提供写操作，也不发起 live Run/evaluate/compare/reproduce/promote/rerun/release。`VITE_CONTROL_PLANE_DATA_SOURCE=fixture` 仅用于显式 fixture/test source，正式默认路径不以 static fallback 掩盖 API 错误；missing/corrupt artifact 与 API unavailable 均有 fail-closed 状态。尚未实现 SLA/统计显著性、Provider usage ingestion。历史 RPF-07/RPF-08 桌面 Chromium 1440/1280 browser evidence 已执行；本 RPF-11 仅完成可用 CUA 的 in-app Chromium 305×612 代表性验证，精确 1440/1280 视口未执行；`Real-device verification: not executed`，不据此宣称 iOS/mobile/PWA 体验。
- RPF-08 reviewed Quality Policy/Gate/Decision corpus 是小规模确定性 prototype（1 Policy、2 Gate Evaluations、2 Release Decisions），仅覆盖当前 `rpf-minimal-reliability-suite@1.0.0` 与 Baseline/Candidate；Policy 版本、Rule registry、Policy registry、服务端 decision storage、审批 authority、API ingestion、自动发布/部署和跨 Suite 统计质量仍未实现。`ELIGIBLE` 仅为 evidence-based release consideration，不是 release/deploy authorization。
- RPF-09 阶段已证明 H2 file candidate 的本地 relational persistence 与 Spring/Python 同步 boundary；当时本机 Docker daemon 未运行且无 `psql`，所以 RPF-09 本身没有证明 PostgreSQL dialect/operations/HA/backup/performance。RPF-09 的 Control Plane 只保存 metadata summary + verified artifact ref，未引入 auth、tenant、approval、CI ingestion、Queue、scheduler、durable execution 或 release endpoint；PostgreSQL-specific 边界由独立 RPF-10 candidate 补充验证。
- RPF-10 只在 Docker Desktop 本地 PostgreSQL 16.15 candidate 上证明当前 schema/operation/recovery/auth boundary；未证明 production HA/replication、跨租户身份、完整用户权限/OAuth/OIDC/SSO、secret manager、backup retention/SLA、性能容量、Approval service、CI provider 或 durable job execution。Bearer credential 是 disposable service-to-service candidate，不能直接作为生产认证方案。
- RPF-11/RPF-14 formal slice 仍是 Stabilization 范围的最小 backend：artifact backend 目前为 local filesystem、service auth 为环境注入的 bearer service credential，未证明 production HA/replication、object storage、secret manager、OAuth/OIDC/SSO、tenant/RBAC、backup retention/SLA 或性能容量；没有 broker、scheduler/autoscaling、production HA、Approval 或 release/deploy endpoint。正式 API 已接入 Web，RPF-14 Execution 页面只读；精确 Chromium 1440/1280 视口和真实设备验证受当前 CUA surface 限制未执行。
- RPF-12 GitHub Actions 已证明 hosted-runner 级 CI/evidence/gate 连接，但 PostgreSQL、Control Plane 与 local artifact store 均为每次 run 的临时实例，未证明跨 run durable evidence retention、production secret manager、HA、tenant/RBAC、Approval 或 branch protection；GitHub 对同一 run 的 rerun 可能只保留最新 attempt artifact，长期历史留存仍需独立策略。当前 attempt/correlation-scoped identity、Control Plane immutable Decision history 与同一 Decision 的 idempotent replay 已通过。
- RPF-13 durable execution/job transport spike 仍作为历史 prototype candidate 保存；RPF-14 已在正式 Control Plane、Python worker 和 RPF-12 CI 中关闭最小 PostgreSQL poll/claim/lease/reconcile 边界，但没有证明 production HA/replication、retention/backup policy、容量/性能、multi-region、scheduler/autoscaling、broker、user/tenant/RBAC、Approval、real production side effect 或 deploy/release authority。Candidate B broker 未实现，不能把当前“不需要 broker”的判断写成永久架构禁令。
- RPF-14 的 retention 只定义 terminal Job 保留、Attempt/Event append-only、execution/evidence 独立保留与无静默 delete endpoint；尚未实现完整 retention deletion policy、object store、backup retention/SLA、scheduler、worker deployment/autoscaling 或 rollout/version compatibility。当前 worker 执行的是 deterministic Evaluation profiles，不是跨进程 DeepSeek thinking/streaming continuation，也不连接真实 Production destructive credentials。CI 使用 ephemeral PostgreSQL/Control Plane/local artifact store；跨 run durable evidence retention 仍需后续 Production-readiness Checkpoint。
- RPF-15 已证明 Candidate A 的本地 production-like 恢复边界，但它仍有 single-host failure、local artifact host-loss/off-host backup、单区域/no HA、手工运维与容量限制；Candidate B 的 managed PostgreSQL/object storage/secret manager、provider IAM、retention/restore SLA、TLS/domain/network、用户身份/Approval、branch protection、rollout/rollback/capacity 与真实 deploy principal 尚未实现或验证。`0.1.0-rc.1` 与 `rpf-v0.1.0-rc.1` 只是 probe recommendation，未创建 tag；RPF-15 未执行真实 Production deploy/release。
- RPF-07/RPF-08/RPF-16 reviewed corpus 是小规模确定性 prototype（分别为 Production Change 3 members × 2 versions、Incident Remediation 3 members × 2 versions，另含各自 Gate/Decision outcomes），只证明当前合同/证据链、对比和 Release Decision precedence 语义，不证明真实 Provider 长期成本、延迟分布、统计显著性或大规模 suite 调度性能；build 有 Vite chunk size warning，但构建成功。
- RPF-18 reviewed statistical corpus 是小规模、确定性、受控序列（Stable 20/20、Baseline 10/20、Flaky 17/20、Safety 19/20 含 1 个 zero-tolerance event、Evidence-poor 8/20 valid Agent trials）；它证明 denominator、Wilson edge/small-sample、interval comparison、flaky observation、safety precedence 与 durable replay 合同，不证明真实 Provider failure probability、长期成本/延迟分布、统计显著性、adaptive sampling 或大规模调度容量。正式 PostgreSQL probe 20 个 Trial Job 均 `COMPLETED`、attempt `1`、unique Run `20`，但仍是单主机/临时 Control Plane、local artifact store 与 deterministic Agent simulation；真实设备 Web 验证未执行。
- RPF-19 Golden Demo 是本地/面试级 reviewed-evidence delivery surface，不是 production deployment；其 profile、seed、single-host PostgreSQL volume、local credentials 与 artifact copies 仍属于本机运行状态，Web 依赖可用的正式 Control Plane API，API 不可用时 fail closed。Overview 当前复用既有 shared bundle，Vite chunk-size warning 尚未通过 lazy route split 消除；真实设备、云资源、长期 Provider 稳定性与 production HA/identity/Approval 仍未验证。
- RPF-21 当前限制：本 Plan 的 fresh PostgreSQL formal probe、local canonical Release Gate、真实 GitHub-hosted gate 与 Docker integration tests 已完成；RPF20-F05、F07、F08、F09～F12、F14～F19 仍在本轮范围之外，不因 RPF-21 Complete 而晋升为已实现能力。
- RPF-22 当前限制：eligible discovery 仍是 bounded PostgreSQL HTTP polling，不是 broker、scheduler、backpressure、autoscaling 或容量方案；artifact store 仍是单机 local filesystem，junction/reparse containment 与 single-read verified DTO 已在当前 Windows 验证，但不等于 off-host object storage、跨主机原子写入、backup retention/SLA 或 Production HA；candidate writer 仍不授予 canonical verified/release authority。
- RPF-23 当前限制：child environment、worker IO root、state v2 process/session identity、rollback、ownership label 与 RemoveData boundary 已在当前 Windows 普通用户本机 fresh 验证；这不是 OS sandbox、Windows Service、Production isolation/HA、managed secret manager、跨主机 process supervisor 或完整 runtime/container security boundary。legacy 无 label RPF-19 resource 只能安全复用/停止，不能由 RPF-23 自动 destructive cleanup；Web/Golden Demo 仍依赖正式 Control Plane 可用并在 API 不可用时 fail closed。
- RPF-06 focused evidence 使用 deterministic known-bad/fixed profiles，因此本轮 promotion/focused rerun 不需要新增 DeepSeek Provider 调用；真实 Provider failure/continuation 未由该路径重新验证。
- 401/402/422/429/5xx/transport 等主要为 documented + synthetic 分类证据，不宣称真实触发；secret 防护不是完整生产 DLP，成本估计不是账单。Java/Python 等推荐架构未被此次 Node probe 决定或否定。
- TU-008 Agent Integration Contract 已由 RPF-16 在两个独立 Agent/domain 上以窄 explicit registry、scenario/tool/environment/verifier compatibility 与 cross-Agent evaluation evidence 关闭当前最小边界；不代表通用 SDK/plugin/marketplace。TU-010 Failure Minimization 待更大真实 Failure Corpus 后再调查，不阻塞当前 slice。


- RPF-24 当前限制：已在当前 Windows CUA 的 desktop viewport 完成代表性浏览器验收，但真实设备未执行；Vite shared bundle warning 仍存在，lazy route split 留作后续低风险优化；App 中未纳入本轮 feature ownership 的历史只读页面仍使用现有 corpus/read-model 边界，下一步 i18n 不应改变 canonical truth 或 raw enum 合同。
- RPF-25 当前限制：双语覆盖的是 Desktop Web presentation layer，真实设备/iOS/mobile/PWA 未执行；legacy `App.tsx` 只读 surface 仍通过集中 legacy copy inventory + DOM compatibility bridge 兼容迁移，后续公共文档本地化与更广泛产品 copy 仍需独立 Plan；Vite shared bundle warning 未因 i18n 扩大而解决。未引入 backend/API locale negotiation、第三 locale、schema/reviewed artifact 改写、release/deploy 或真实 Provider 调用。

## Active work

RPF-25 已完成双语 presentation-only Control Plane Web i18n，状态为 `Complete`；继续保持 `Feature Freeze / Portfolio Maintenance`，不扩大到 broker、scheduler、容量或 Production release。RPF-24 的 Web canonical truth 修复与最小模块化、RPF-23 的 child least privilege、worker path containment 与 Golden Demo process/resource lifecycle hardening、RPF-22 的 durable eligible discovery/canonical artifact integrity、RPF-21 的 fresh PostgreSQL formal probe/local canonical Release Gate/真实 hosted gate 与此前 Docker integration tests 继续有效。
RPF-19 已完成；当前进入 `Feature Freeze / Portfolio Maintenance`，RPF-25 的实现已完成，等待本次交付记录落地后无进行中的产品实现任务。RPF-09 已把项目 lifecycle 从 `Prototype` 推进到 `Stabilization`，RPF-10 在真实 PostgreSQL 与 disposable auth candidate 范围闭合 persistence/recovery/authority 的最小验证，RPF-11 将这些合同落为正式 Control Plane/API/client/Web slice，RPF-12 将其接入真实 GitHub Actions hosted-runner gate，RPF-13 在 disposable candidate 范围解锁 TU-004/TU-005 的正式实现边界，RPF-14 将最小 durable Job/worker/CI/read-only execution slice 落地，RPF-15 完成首个 production-like Candidate A 证据与 Candidate B 拓扑/release 边界比较，RPF-16 完成第二真实 Incident Remediation Agent 与窄 cross-Agent Reliability Contract，RPF-17 完成 deterministic Failure Intelligence 与只读调查面，RPF-18 完成 additive Statistical Evaluation、受控 flaky/safety/evidence-poor corpus、Wilson comparison、durable Trial Job path 与只读统计调查面，RPF-19 完成 Golden Demo profile、幂等 seed/start/stop、API-backed Overview、证据链 deep links 与面试级交付文档；RPF-25 固化 D-024 的双语 presentation-only locale、技术词/raw boundary 与不改变 canonical truth 的 i18n 规则；仍不把 local artifact store、Bearer credential、同步 HTTP、当前 worker 或 RPF-13 candidate 写成 production HA、Approval、broker、scheduler 或 release architecture。D-013 固化 Quality Policy precedence 与 decision-only 边界，D-014 固化 Stabilization 的 metadata/artifact/reference/API 最小边界，D-015/D-016 固化 PostgreSQL 与外部 decision writer 登记边界，D-017/D-018 固化 fenced durable execution 与 PostgreSQL poll/claim transport recommendation，D-019 固化 submitter/worker/decision-writer authority 与 durable path no-fallback 边界，D-020 固化 managed persistence/stateless Production target 与显式 Release 边界，D-021 固化第二 Agent 的 explicit narrow integration contract 与独立 Incident evidence domain，D-022 固化确定性 Failure Intelligence、结构族与 fail-closed version bisect 边界，D-023 固化 additive statistical sampling、Wilson、denominator、flaky/safety gate 与 controlled-corpus 边界，D-024 固化 locale 与 presentation/raw boundary。

## Next likely boundary

RPF-25 已收口为 `Complete`；下一工作边界是 bilingual public documentation / public copy（需新 Plan），不从本轮证据推导 Production HA、Approval、broker、scheduler、autoscaling 或 release/deploy 授权。Web 的 canonical snapshot、selector、feature ownership、shared status/data-source/CSS 与 presentation-only locale 边界已建立；不得把本机 lifecycle hardening 或本轮 Web i18n 直接升级为 Production security architecture。
当前优先保持 `Feature Freeze / Portfolio Maintenance`：Golden Demo 作为本地/面试交付面维护；如需进入 Production implementation Plan，必须在明确 provider/region 与授权后，落地 managed PostgreSQL、object storage、secret manager、Product Release Identity registry、用户身份/Approval、显式 release/deploy principal、backup retention/restore SLA、rollout/rollback、capacity/backpressure 与 observability evidence；在这些条件具备前保持 `Stabilization`，不执行真实 deploy/release，也不引入 broker、scheduler、autoscaling 或 HA。RPF-17 的 Failure Intelligence、RPF-18 的 Statistical Evaluation 与 RPF-19 的 Golden Demo 当前仍基于小规模 deterministic/controlled reviewed corpus，不证明生产规模聚类、真实 Provider failure probability、统计显著性或真实 remediation；第三 Agent、通用 SDK/plugin、LLM/embedding/ML 辅助分析、live stochastic cohort 与真实 Incident remediation 需新 Plan/新证据。

## Update rule

在完成、Partial、Blocked 或交接时仅按 material change 更新当前事实；目标不能写成能力，当前限制不能改写成永久 non-goal。最终提交身份与同步状态以实时 Git/远端核验为准，不维护旧 SHA 流水或将 Optional USER CHECK 纳入待办。
