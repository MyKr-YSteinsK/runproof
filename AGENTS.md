# AGENTS.md

> RunProof 仓库执行规则；稳定产品合同、决定与当前事实分别由下列 Project State 文件持有。

## 1. Project identity and context

- RunProof (`RPF`)；repository name：`runproof`；主分支：`main`。
- GitHub identity：`MyKr-YSteinsK/runproof`，visibility：`Public`（用户明确确认）；实际 remote/upstream 接入状态见 CURRENT_STATE。
- 唯一 Project State 位置：`docs/project/PROJECT_BRIEF.md`（稳定产品/UX 合同）、`DECISIONS.md`（canonical D-001～D-012 及后续决定）、`CURRENT_STATE.md`（当前阶段、能力、限制与交付事实）。
- 完整 Bootstrap 由 Architect 持有；后续 Plan 必须带入相关 MUST、Acceptance、UX invariant。当前实现或摘要遗漏不能自行废除目标需求；事实冲突核对环境/版本，产品取舍按明确决定处理。

## 2. Protected / private / external boundaries

- 保护无关用户文件和改动；不提交 secrets、私密数据、临时输出、构建/覆盖率产物或本地运行目录。
- DeepSeek API Key、未来 Provider credentials、authorization token 不得进入 Git、前端源码、Scenario、默认持久化 Trace 或普通 Evidence export；敏感字段默认 redact/mask。本地 secret 防护见 `.gitignore`。
- v1 Agent 有副作用 Tool 只操作 Controlled Production Simulation Environment，不连接真实生产 destructive credentials。产品的 Simulation 范围不自动授权执行器在任意任务中执行 destructive/live 操作。
- 关键 Release Authority 不由被测 Agent 持有；Agent/runtime 不得绕过 Approval/Quality Policy。`ELIGIBLE` 不是已发布状态，也不是 deploy/release 授权。
- 不因推荐技术栈搭建完整空壳或引入 Queue/容器编排；未知架构边界先按任务开展 Investigation/Spike。

## 3. Repository command semantics

- RPF-01 disposable probe 使用 Node.js 24 内置 API，无第三方依赖；不代表正式 Runtime 的语言决定。
- 本地合同测试：`node --test spikes/rpf-01/probe.test.mjs`；固定证据离线校验：`node spikes/rpf-01/verify-evidence.mjs`。两者都不调用 Provider，前者检查拒绝/故障路径，后者核对实际样本与源码身份。
- RPF-02 host reference：`node --test spikes/rpf-02/probe.test.mjs`、`node spikes/rpf-02/verify-evidence.mjs`、`node spikes/rpf-02/probe.mjs --run`；真实 Docker provider：`node --test spikes/rpf-02/docker-probe.test.mjs`、`node spikes/rpf-02/docker-probe.mjs --run`、`node spikes/rpf-02/verify-docker-evidence.mjs`。Docker probe 只使用现有 daemon/image，创建带 `rpf02` 前缀的临时 container/volume 并验证 cleanup；Docker 证据证明 Prototype-level contract，不等于 production-grade isolation/HA。
- RPF-03 product runtime deterministic tests：`python -m unittest discover -s runtime/tests -p 'test_*.py' -v`；reviewed artifact 离线核验：`python runtime/verify-reviewed-artifacts.py`；live normal：`python -m runtime.runproof_runtime --fault none`；live response-lost：`python -m runtime.runproof_runtime --fault response-lost`。live 命令从 `DEEPSEEK_API_KEY` 读取 secret、使用 Docker Fresh-per-run，并将每次独立 artifact 写入忽略的 `.local/rpf-03/`；不保存 messages、Authorization、private reasoning 或自由模型文本。
- 真实实验：`node spikes/rpf-01/probe.mjs --live`，仅在当前任务授权 API 调用与费用时运行，从进程环境读取 `DEEPSEEK_API_KEY`，绝不打印 key 或私有 continuation。
- 临时输出仅在忽略的 `.local/rpf-01/`；`spikes/rpf-01/reviewed-evidence.json` 是经过脱敏审查的验收固定样本，允许提交，不能自动以新实验覆盖。修改 probe 后需区分旧证据与新源码身份。
- RPF-02 临时输出仅在忽略的 `.local/rpf-02/`；`spikes/rpf-02/reviewed-evidence.json` 是历史 host-reference 固定样本，`spikes/rpf-02/reviewed-docker-evidence.json` 是真实 Docker provider 脱敏固定样本，允许提交，不能自动以新实验覆盖。修改任一 probe 后需重新生成并审查对应样本的 `source_sha256`；Docker 是当前证据 provider，不是永久产品身份。
- RPF-03 `.local/rpf-03/` 只保存本地 Run artifacts，不提交；runtime source identity 由正式 package 文件 hash 记录。live artifacts 必须经过 secret/private-protocol redaction，历史 Run 不得静默覆盖。
- RPF-03 `runtime/reviewed-*.json` 是经过审查的固定 live evidence 样本，允许提交；`runtime/verify-reviewed-artifacts.py` 校验 artifact schema、PASS、Docker cleanup、response-lost reconcile、secret/private-protocol 边界与 runtime source hash。修改 `runtime/runproof_runtime/` 后必须重新生成并审查样本，不能让旧样本冒充新源码证据。
- RPF-04 Web product commands：`npm test`、`npm run typecheck`、`npm run build`；Web 只从 `runtime/reviewed-*-run-v2.json` 读取 reviewed corpus，不发起 live Run，不保存 secret、Authorization 或 private reasoning。RPF-04 `.local/rpf-04/` 只保存重新生成 v2 artifacts 的本地副本，不提交；固定 v1/v2 样本身份不得静默覆盖。
- RPF-05/RPF-06 runtime：同一 `python -m unittest discover -s runtime/tests -p 'test_*.py' -v`、`python runtime/verify-reviewed-artifacts.py` 合同继续适用；RPF-06 的 `--check-promotion`、`--promote-failure-case`、`--focused-regression`、`--verify-regression` 分别覆盖 gate、显式 promotion、focused rerun 与 Regression artifact 校验。RPF-06 使用 Docker Fresh-per-run，临时输出仅在忽略的 `.local/rpf-06/`，reviewed Stability/Focused Runs、Regression、Gate、Result、Collection 与 promoted Failure Case 不得静默覆盖历史 RPF-04/RPF-05 样本。
- RPF-06 Web 继续使用 `npm test`、`npm run typecheck`、`npm run build`；`/regressions` 与 `/regressions/:regressionId` 只读消费 reviewed Regression corpus，并通过 stable refs 链接 Failure Case、source/reproduction/focused Runs 与 events，不发起 live Run、promote、rerun 或 mutate。
- RPF-07 runtime：继续使用上述 runtime tests/verifier；`--build-suite`、`--validate-suite`、`--evaluate-suite`、`--compare-baseline-candidate`、`--verify-evaluation`、`--verify-comparison` 覆盖 Suite、独立 Evaluation 与 Comparison contract。RPF-07 仅使用 deterministic profiles + Docker Fresh-per-member，临时输出仅在忽略的 `.local/rpf-07/`；reviewed Suite、6 条 member Runs、2 份 Evaluation、2 份 Regression result 与 Comparison 绑定当前 RPF-07 source hash，不得静默覆盖 RPF-01～RPF-06 历史样本；不调用 Provider。
- RPF-08 runtime：继续使用上述 runtime tests/verifier；`--validate-policy`、`--evaluate-quality-gate`、`--create-release-decision`、`--verify-quality-gate`、`--verify-release-decision` 覆盖 Quality Policy、Gate Evaluation 与 Release Decision contract。RPF-08 仅读取 RPF-07 deterministic reviewed corpus，不创建新的 Docker resource 或调用 Provider；临时输出仅在忽略的 `.local/rpf-08/`，reviewed Policy、2 份 Gate 与 2 份 Decision 绑定当前 RPF-08 source hash，不得静默覆盖 RPF-01～RPF-07 历史样本；`ELIGIBLE` 仍是 decision-only，不提供 deploy/release command。
- RPF-09 disposable Control Plane probe：`mvn -q test package -f spikes/rpf-09/control-plane/pom.xml`、`python spikes/rpf-09/probe.py` 覆盖 Spring Boot/JDK candidate、H2 file persistence、schema migration、manifest ingestion、idempotency/conflict、transaction rollback、service restart、artifact ref/hash/schema/source validation 与 Python→Java HTTP/JSON 跨进程路径。输出仅在忽略的 `.local/rpf-09/`，不修改 RPF-01～RPF-08 reviewed artifact；本 probe 不是正式后端、PostgreSQL、Queue、scheduler、durable worker 或 release/deploy authorization。
- RPF-10 disposable Control Plane/PostgreSQL/auth probe：`mvn -q test package -f spikes/rpf-10/control-plane/pom.xml`、`python spikes/rpf-10/probe.py --run`、`python spikes/rpf-10/verify-evidence.py` 覆盖真实 Docker `postgres:16-alpine`、named-volume persistence、PostgreSQL migration/insert/query/rollback/idempotency/conflict/concurrency/restart/backup-restore、schema mismatch、storage failure 与 service-to-service read/evidence/decision/agent authority boundary。credentials 仅在 probe/service/container 环境，输出仅在忽略的 `.local/rpf-10/`；`spikes/rpf-10/reviewed-evidence.json` 为脱敏固定样本，source identity 由 verifier 校验。该 probe 不是正式后端、用户登录/OAuth/SSO、Approval、CI provider、Queue、scheduler、durable worker 或 release/deploy authorization。
- RPF-11 正式 Control Plane：`mvn -q test package -f control-plane/pom.xml`、`python control-plane/probe.py` 覆盖 Java/Spring + PostgreSQL canonical metadata、versioned schema history、immutable local `ArtifactStore`、completed-result manifest ingest、idempotency/conflict/concurrent race、transaction rollback、append-only/superseding Release Decision、service/read/evidence/decision/agent/CI authority、artifact fail-closed、service/DB restart 与 `pg_dump`/独立 `pg_restore`。RPF-14 probe 使用现有 Docker daemon 创建带 `rpf14` 前缀的临时资源，输出仅在忽略的 `.local/rpf-14/`；正式 Python client 为 `python -m runtime.runproof_runtime.control_plane_client`，只走 HTTP/JSON，不直连 DB。正式模块仍不包含 OAuth/OIDC/SSO、tenant/RBAC、Approval、broker、scheduler、production HA 或 release/deploy authorization。
- RPF-12 真实 GitHub Actions Release Gate：`.github/workflows/release-gate.yml` 由 `push(main)`、`pull_request(main)` 与 `workflow_dispatch` 触发；先执行 repository contract checks，再运行 `python ci/run_release_gate.py`，在 GitHub-hosted runner 内 fresh 启动 PostgreSQL + 正式 `control-plane/`，由正式 durable worker 执行 Baseline/Candidate，完成独立 principal authority checks、canonical Decision read-back，并上传脱敏 `ci-results/rpf12-release-gate-result.json`、Summary mirror，同时写入并校验 GitHub Job Summary。workflow 只有 `contents: read`、`actions: write`，不执行 release/deploy；RPF-12 的真实 run、artifact、summary 与 remote delivery facts 仅以 CURRENT_STATE 为准。
- RPF-13 disposable Durable Run Execution / Job Transport probe：`mvn -q test package -f spikes/rpf-13/control-plane/pom.xml`、`python spikes/rpf-13/probe.py --run`、`python spikes/rpf-13/verify-evidence.py` 覆盖真实 Docker `postgres:16-alpine`、PostgreSQL durable job/attempt/operation/evidence tables、row-lock claim race、lease/heartbeat/expiry、stale fencing、跨 worker process `UNKNOWN_OUTCOME` reconcile、duplicate delivery、artifact ingest replay、cancellation/timeout、DB/service restart、future CI submit/poll/read 与 secret/private-protocol boundary。输出仅在忽略的 `.local/rpf-13/`；`spikes/rpf-13/reviewed-evidence.json` 为脱敏固定样本，source identity 由 verifier 校验。该 probe 推荐 PostgreSQL poll/claim candidate 进入下一正式 Plan，但不是正式 durable worker、broker、scheduler、production HA 或 release/deploy authorization。
- RPF-14 正式 Durable Execution：`mvn -q test package -f control-plane/pom.xml`、`python control-plane/probe.py`、`python control-plane/verify-evidence.py --result <probe-result.json>` 覆盖 additive `rpf-14-durable-execution-schema-v1`、正式 Job/Attempt/Operation/Event/Evidence API、独立 Python worker、submit/replay/conflict、concurrent claim、heartbeat/lease/reclaim/stale fencing、UNKNOWN_OUTCOME cross-process reconcile、artifact-ingest recovery、cancel/timeout、service/DB restart、backup/restore、schema mismatch、read API/metrics/retention 与 auth boundary。输出仅在忽略的 `.local/rpf-14/`；probe 创建带 `rpf14` 前缀的临时 PostgreSQL/container/volume/JVM 并验证 cleanup；worker 为 `python -m runtime.runproof_runtime.durable_worker`，只走 HTTP/JSON，不直连 DB。该正式 slice 不包含 broker、scheduler/autoscaling、production HA、OAuth/OIDC/SSO、tenant/RBAC、Approval 或 release/deploy authorization。
- RPF-15 Production-readiness investigation：`mvn -q test package -f control-plane/pom.xml`、`npm run build`、`python spikes/rpf-15/probe.py`、`python spikes/rpf-15/verify-evidence.py --result <probe-result.json>` 覆盖单主机 production-like Candidate A 的 Web/Control Plane/Worker/PostgreSQL/Artifact Store、process/container replacement、active durable job fencing/reconcile、PostgreSQL named-volume replacement + `pg_dump`/独立 `pg_restore`、immutable artifact backup/restore、additive migration/application rollback、Product Release Identity、secret injection/rotation/redaction、auth/Approval、observability/readiness 与 explicit-release boundary。输出仅在忽略的 `.local/rpf-15/`；不创建云账号/付费资源、不执行真实 Production deploy/release，不把 Candidate A 写成 HA/Production ready；Candidate B managed PostgreSQL/object storage/stateless topology 只做结构化比较。
- RPF-16 cross-Agent contract：`python -m unittest discover -s runtime/tests -p 'test_*.py' -v`、`python spikes/rpf-16/probe.py --build-reviewed`、`python spikes/rpf-16/probe.py --verify`、`python spikes/rpf-16/probe.py --run`、`python spikes/rpf-16/verify-evidence.py` 覆盖显式 Production Change / Incident Remediation Agent contract、Incident local/external/response-lost 三类场景、Failure Case reproduction、两条 stability、Regression promotion/focused rerun、独立 Suite/Evaluation/Comparison/Quality Policy/Gate/Decision 与正式 PostgreSQL durable worker Candidate 执行。`--build-reviewed --refresh-reviewed` 是源码变更后的显式刷新动作，只允许刷新 RPF-16 reviewed 文件及其本地 ignored build metadata；默认 immutable writer 仍拒绝不同字节。RPF-16 使用 deterministic controlled process-local Incident simulation；formal `--run` 使用带 `rpf16` 前缀的临时 PostgreSQL/Control Plane/worker 并验证 cleanup，不调用 Provider，不连接真实 Production，不执行 release/deploy。
- RPF-07 Web 继续使用 `npm test`、`npm run typecheck`、`npm run build`；`/evaluations`、`/evaluations/:evaluationId`、`/comparisons`、`/comparisons/:comparisonId` 只读消费 reviewed Suite/Evaluation/Comparison corpus，通过 stable refs 链接 Run/Regression；页面不发起 live Run、evaluate、compare、promote、rerun 或 mutate，不输出 Release Decision。
- RPF-08 Web 继续使用 `npm test`、`npm run typecheck`、`npm run build`；`/release-decisions`、`/release-decisions/:decisionId` 只读消费 reviewed Quality Policy/Gate/Decision corpus，通过 stable refs 链接 Evaluation/Comparison/Regression/Run；页面不发起 live Run、evaluate、compare、promote、rerun、release 或 mutate，不提供 Deploy now，也不把 `ELIGIBLE` 显示为 Released。
- RPF-09 Web migration 只验证 API-style `canonical_metadata` + verified `artifact_resolution` DTO 到现有 Release Decision list view model 的薄 adapter 兼容性；不把 Web 正式切换为 API-backed live application，不让 presentation components 依赖数据库字段。
- RPF-11 Web：继续使用 `npm test`、`npm run typecheck`、`npm run build`；默认由 `web/src/data/controlPlaneApi.ts` 从 `/api/v1` 读取完整 canonical snapshot，并在所有 metadata/artifact 请求成功且 verified 后激活既有 view-model。API/artifact 不可用时 fail closed，不回退静态 corpus；Vite 仅在 dev proxy 中从进程环境注入 read token，前端 bundle 不含 credential。`VITE_CONTROL_PLANE_DATA_SOURCE=fixture` 仅为显式测试模式。
- RPF-14 Web：继续使用 `npm test`、`npm run typecheck`、`npm run build`；`/executions` 与 `/executions/:jobId` 只读消费 `/api/v1/jobs`、`/execution-metrics` 的 canonical Job/Attempt/Operation/Evidence/Event DTO，不提供 claim/cancel/rerun/promote/evaluate/release/mutate 操作。API 不可用、DTO malformed 或 artifact 未 verified 时 fail closed；Execution state 与 Agent outcome 分开显示，`RECONCILE_REQUIRED` 明确禁止 blind retry，raw JSON 仅作 expert escape hatch。浏览器不持有 worker/decision credential。
- RPF-16 Web：继续使用 `npm test`、`npm run typecheck`、`npm run build`；`/agents` 与 `/agents/:agentId` 只读消费 verified Agent-bearing corpus，展示两份显式 Agent identity、domain/type、contract、version/config、Scenario/Tool/Environment/Verifier refs、linked Runs/Failures/Regressions/Evaluations/Decisions 与 Incident Facts/Verified/Inference 分层。既有 Run/Failure/Regression/Evaluation/Comparison/Release Decision 页面同时显示 Agent identity/domain；页面不发起 live Agent、mutation、rerun、promote、evaluate、compare 或 release。
- RPF-17 Failure Intelligence：`python -m unittest discover -s runtime/tests -p 'test_*.py' -v`、`python spikes/rpf-17/probe.py --build-reviewed`、`python spikes/rpf-17/probe.py --verify`、`python spikes/rpf-17/probe.py --run`、`python spikes/rpf-17/verify-evidence.py` 覆盖确定性 attribution、first-divergence、domain/cross-Agent family signature、exact dedup/cluster、Failure Case/Run 语料、fail-closed version bisect、negative controls 与既有 PostgreSQL Control Plane artifact/metadata API。`rpf-failure-signature-v1` 的旧 `failure_signature()` 语义和历史 signature bytes 不变；RPF-17 只复用现有 immutable artifact store/canonical metadata，不新增数据库表，不调用 LLM/embedding/ML，不自动 promotion、release 或 deploy。临时输出仅在忽略的 `.local/rpf-17/`；reviewed RPF-17 files 不能静默覆盖旧 RPF-01～RPF-16 corpus，源码变更后必须重新生成并审查 `source_sha256`。
- RPF-17 Web：继续使用 `npm test`、`npm run typecheck`、`npm run build`；`/failure-intelligence`、`/failure-intelligence/clusters/:clusterId`、`/version-bisects/:bisectId` 与既有 Failure Case detail 中的 RPF-17 analysis panel 只读消费 verified deterministic Failure Intelligence/Cluster/Bisect corpus 或 Control Plane DTO，展示 Facts / Verified / Derived / Inference / AI Analysis 分层、exact-versus-structural grouping、occurrences、first bad/recommendation 与 no-auto-promotion 边界；页面不发起 live analysis、mutation、rerun、promote、evaluate、compare、release 或 deploy。
- RPF-18 Statistical Evaluation：`python -m unittest discover -s runtime/tests -p 'test_*.py' -v`、`python spikes/rpf-18/probe.py --build-reviewed --refresh-reviewed`、`python spikes/rpf-18/probe.py --verify`、`python spikes/rpf-18/probe.py --run`、`python spikes/rpf-18/verify-evidence.py` 覆盖 additive `rpf-statistical-sampling-plan-v1`、`rpf-statistical-evaluation-v1`、`rpf-statistical-comparison-v1`、独立 Statistical Policy/Gate/Decision、Wilson score interval、Agent/evidence denominator、flaky observation、zero-tolerance safety、RPF-17 Failure Intelligence linkage、controlled corpus 与真实 PostgreSQL durable Trial Job/worker/read-back。`--build-reviewed --refresh-reviewed` 是源码变更后的显式刷新动作，只允许刷新 RPF-18 reviewed 文件及其 ignored controlled runs；历史 RPF-01～RPF-17 reviewed bytes 不得静默覆盖。RPF-18 使用 deterministic controlled sequence，不调用 DeepSeek、不宣称 live probability；formal `--run` 使用带 `rpf18` 前缀的临时 PostgreSQL/Control Plane/worker 并验证 cleanup，不执行 release/deploy。
- RPF-18 Web：继续使用 `npm test`、`npm run typecheck`、`npm run build`；`/statistical-evaluations`、`/statistical-evaluations/:evaluationId`、`/statistical-comparisons`、`/statistical-comparisons/:comparisonId` 只读消费 verified Statistical corpus 或 Control Plane DTO，展示 trial matrix/drilldown、attempted 与 Agent denominator、Wilson interval、flaky/safety/failure-family、policy rule reasons、comparison classification 与 decision-only boundary；页面不发起 live trial、rerun、promote、evaluate、compare、release 或 deploy。
- RPF-19 Golden Demo：`python demo/verify-golden-demo.py --root . --json` 校验 versioned profile、source/artifact hashes、双 Agent 与 reviewed refs；`python demo/seed_demo.py --root . --base-url http://127.0.0.1:8081 --artifact-root .local/rpf-19/demo-artifacts --json` 通过正式 HTTP/JSON Control Plane client 幂等注册并 read-back reviewed corpus；`powershell -ExecutionPolicy Bypass -File demo/start-demo.ps1` / `demo/stop-demo.ps1` 只操作明确的本机 `rpf19-demo-*` PostgreSQL/container/volume、正式 Control Plane 与 Vite 进程，运行状态与凭据仅在 ignored `.local/rpf-19/`。Golden Demo 不调用 Provider、不需要云/付费资源、不执行 release/deploy；历史 reviewed bytes 不静默刷新。
- RPF-19 Web：继续使用 `npm test`、`npm run typecheck`、`npm run build`；`/` 与 `/overview` 是 API-backed、read-only Golden Demo Overview，沿 Candidate→Evidence→Failure→Regression→Gate 链接现有 Agent/Run/Failure/Regression/Evaluation/Statistical/Decision/Execution drilldown；默认 API corpus 不可用时 fail closed，只有显式 `VITE_CONTROL_PLANE_DATA_SOURCE=fixture` 测试模式可使用 fixture；Overview 不发起 live Agent、rerun、promote、evaluate、compare、release、deploy 或 mutation。
- RPF-22 Durable discovery / Artifact Integrity：`mvn -q test package -f control-plane/pom.xml`、`python control-plane/probe.py`、`python control-plane/verify-evidence.py --result <probe-result.json>`、`python -m unittest discover -s runtime/tests -p 'test_*.py' -v`、`python runtime/verify-reviewed-artifacts.py` 与 Web 三项命令覆盖 server-side `eligible=true` bounded discovery、terminal-history starvation、expired-lease reclaim、additive PostgreSQL discovery index、local artifact root traversal/absolute-path/symlink-or-junction containment、single-read verified artifact DTO/controlled mutation race、immutable candidate writer 与 verifier authority boundary。输出仅在忽略的 `.local/rpf-14/` 或 `ci-results/`；不刷新历史 reviewed corpus，不引入 RPF-23，也不扩大到 broker、scheduler、capacity、object storage、OAuth/OIDC/SSO、Production deploy/release。
- RPF-23 Demo least privilege / lifecycle：Windows 本机执行 `powershell -ExecutionPolicy Bypass -File demo/verify-lifecycle.ps1 -Mode contracts` 与 `-Mode lifecycle` 覆盖 child-specific environment、worker `--allowed-root` path containment、state v2 process/session identity、foreign port/process/container negative、control-plane/seed/web/state-write rollback、PID reuse、restart/stop/RemoveData、Golden Demo verifier 与 secret scan。`demo/start-demo.ps1` 每次启动都会在 ignored local log 中 fresh package 正式 Control Plane；Web 仅获得 read token，Worker 仅获得 worker token 与显式 IO roots，state/log/bundle 不保存 secret。新建 Docker resource 使用 `com.runproof.owner=runproof`、`com.runproof.demo=rpf19-golden-demo`、`com.runproof.lifecycle=rpf-23` labels；历史无 label 的 exact RPF-19 resource 可安全复用/停止，但 `RemoveData` fail closed 保留数据。该 probe 是当前 Windows 本机 lifecycle evidence，不等于 Production isolation/HA、OAuth/OIDC/SSO、broker、scheduler 或 release/deploy authorization。
- RPF-26 public documentation：`python ci/verify-public-docs.py` 校验 English/简体中文公共文档配对、关键 Reliability/Production/Demo 语义、公共本地链接、命令字符串、绝对路径/credential-shaped literal 与 Project-State 单一事实源；该检查不替代产品测试、浏览器验收或 hosted Release Gate。
- RPF-27 disposable multi-service/network-fault Spike：`python spikes/rpf-27/probe.py --run --repeat 5`、`python spikes/rpf-27/verify-evidence.py` 覆盖 Docker `--internal` private network、Agent-shaped client、Toxiproxy、Envoy HTTP fault feasibility、narrow custom shim、baseline、latency/timeout、dependency unavailable、真实 downstream response-lost、receipt/effect-count reconcile、planned/triggered/observed fault provenance、authority boundary 与 cleanup；需要预先存在的 `python:3.12-alpine`、`ghcr.io/shopify/toxiproxy:2.12.0`、`envoyproxy/envoy:v1.31-latest` images。输出仅在忽略的 `.local/rpf-27/`，每 trial 使用 `rpf27` 前缀、无 volume、无 Golden Demo/PostgreSQL 资源；该 Spike 不替换正式 `DockerEnvironment`、不登记 reviewed corpus、不新增正式 schema、不接 OTel/S3/MinIO/capacity/Kubernetes/broker/scheduler、不执行 Production remediation 或 release/deploy。Hosted candidate workflow 为 `.github/workflows/rpf-27-network-spike.yml`，使用 `--candidate toxiproxy --repeat 3` 并以 `verify-evidence.py --candidate toxiproxy` 校验选定候选的完整场景与三次 response-lost，只产生诊断 artifact，不是 canonical Release Gate。
- RPF-28 formal multi-service Environment：`mvn -q test package -f control-plane/pom.xml`、`python -m unittest discover -s runtime/tests -p 'test_*.py' -v`、`python spikes/rpf-28/probe.py --run`、`python spikes/rpf-28/verify-evidence.py .local/rpf-28/rpf28-formal-result.json` 与 `python runtime/verify-reviewed-artifacts.py` 覆盖 additive `multi-service-toxiproxy-v1`、fresh Docker `--internal` network、target/dependency/Toxiproxy/Agent-shaped client、readiness/initial verification、no fault/latency/timeout/dependency unavailable/response-lost/pre-side-effect profiles、planned/triggered/observed/reconciled provenance、receipt/effect-count、no-blind-retry、8 项实际 negative controls、cleanup/quarantine，以及独立 PostgreSQL + formal Control Plane + durable worker 的 `environment_profile` 传递。默认本地 probe 每类 profile 重复 5 次；`--hosted` 做 baseline/response-lost focused evidence，同时执行同一 negative-control verifier。`python spikes/rpf-28/probe.py --run --build-reviewed --refresh-reviewed` 是显式 reviewed artifact 刷新动作，不能静默覆盖 RPF-01～RPF-27 历史字节。输出仅在忽略的 `.local/rpf-28/`；resource 使用 `rpf28` 前缀、无 volume、Agent 不可见 Control Plane/Docker socket/Toxiproxy control API。正式 slice 不引入 broker、scheduler、autoscaling、service mesh、OTel、S3/MinIO、Production HA、OAuth/OIDC/SSO、tenant/RBAC、真实 destructive remediation 或 release/deploy authorization。Hosted focused workflow 为 `.github/workflows/rpf-28-multi-service.yml`，不是 canonical Release Gate。
- RPF-29 disposable OpenTelemetry Spike：`mvn -q -f spikes/rpf-29/java/pom.xml package`、`python spikes/rpf-29/probe.py --run`、`python spikes/rpf-29/probe.py --run --hosted` 与 `python spikes/rpf-29/verify-evidence.py <result>` 覆盖官方 `otel/opentelemetry-collector-contrib:0.157.0`、真实 PostgreSQL durable Job、Java candidate→Python worker/Agent→RPF-28 Toxiproxy target/dependency 的 W3C `traceparent` 与 allowlisted W3C Baggage propagation、Attempt reclaim、response-lost/reconcile、Collector failure、missing propagation、低 cardinality metrics、敏感字段 allowlist、定向 overhead 与 cleanup。默认本地模式包含 bounded overhead observation；hosted 模式只跑 focused baseline/response-lost/negative-control。输出仅在忽略的 `.local/rpf-29/`；该 Spike 使用手工 OTLP/HTTP JSON candidate，不是正式 Runtime OTel/SDK 集成，不改正式 schema/canonical evidence/release authority，不接 SaaS/长期 retention/alerting/S3/MinIO/capacity benchmark，不调用 Provider、不执行 Production deploy/release。Hosted workflow 为 `.github/workflows/rpf-29-otel-spike.yml`，仅上传脱敏诊断 artifact，不是 canonical Release Gate。
- RPF-30 正式 OpenTelemetry：`mvn -q test package -f control-plane/pom.xml`、`python -m pip install --requirement runtime/requirements.txt`、`python spikes/rpf-30/probe.py --run`、`python spikes/rpf-30/verify-evidence.py .local/rpf-30/local/rpf30-result.json` 覆盖官方 Java OTel BOM `1.65.0`、Python OTel `1.44.0`、真实 PostgreSQL durable Job、Java Control Plane→Python Worker/Agent→RPF-28 multi-service target/dependency 的 W3C `traceparent`/allowlisted Baggage、Attempt reclaim/link、response-lost/reconcile、disabled/healthy/unavailable Collector、bounded async queue、sensitive/cardinality verifier 与 exact cleanup。探针使用固定 deterministic Agent，不调用 DeepSeek/Provider；输出仅在忽略的 `.local/rpf-30/`，hosted focused workflow 为 `.github/workflows/rpf-30-otel.yml`，命令为 `python spikes/rpf-30/probe.py --run --hosted --output-dir ci-results/rpf30` 后接对应 verifier。OTel 仅为 diagnostic signal，不改 canonical schema/evidence/Release authority，不接 Jaeger/Tempo/Grafana、SaaS、long-term retention、Web trace UI、Production deploy/release，不把 Collector 作为启动依赖。
- RPF-31 S3-compatible Artifact Store Spike：`mvn -q package -f spikes/rpf-31/java/pom.xml`、`python spikes/rpf-31/probe.py --run`、`python spikes/rpf-31/verify-evidence.py .local/rpf-31/local/rpf31-result.json` 与 hosted `python spikes/rpf-31/probe.py --run --hosted --output-dir ci-results/rpf31` 覆盖固定 SeaweedFS `4.47` 与 RustFS `1.0.0`、AWS SDK for Java `2.54.16` endpoint/path-style/timeout/checksum boundary、`If-None-Match: *` immutable first create、same-content replay、different-content conflict、same/different concurrent race、one-GET + RunProof SHA-256/schema/identity verification、missing/corrupt/wrong-identity negative、SeaweedFS static read-only/wrong credential、endpoint unavailable、retained-volume restart、orphan/response-lost replay、1 KiB/1 MiB/16 MiB directional size probe、versioning/Object Lock observation 与 exact cleanup。输出仅在忽略的 `.local/rpf-31/` 或 `ci-results/rpf31/`；该 Spike 不替换 `LocalFileArtifactStore`、不迁移历史 reviewed artifacts、不写 canonical PostgreSQL、不依赖 content-addressed key/ETag/versioning/Object Lock、不接真实云账户、HA/replication/lifecycle/CDN/Web upload/capacity/release/deploy。当前本机/hosted evidence 仅用于解锁 additive RPF-32 `S3ArtifactStore` 调查与实现，不等于 Production durability/HA。
- RPF-32 正式 S3-compatible ArtifactStore：`mvn -q test package -f control-plane/pom.xml`、`mvn -q package -f spikes/rpf-32/java/pom.xml`、`python spikes/rpf-32/probe.py --run --output-dir .local/rpf-32/<run>`、`python spikes/rpf-32/verify-evidence.py --result <rpf32-result.json>` 与 `.github/workflows/rpf-32-s3-artifact-store.yml` 的 hosted focused proof 覆盖显式 `local`/`s3` backend wiring、AWS SDK for Java `2.54.16`、SeaweedFS `4.47`、PostgreSQL canonical metadata、conditional `If-None-Match: *` create/replay/conflict/race、bounded unknown-write reconcile、one-GET RunProof SHA-256/schema/identity/source/runtime verification、key containment、missing/corrupt/read-only/invalid-access/unavailable negative、object-store/PostgreSQL restart、put-before-metadata orphan、metadata replay、正式 HTTP/JSON durable Worker S3 upload 与 Agent authority boundary。输出仅在忽略的 `.local/rpf-32/` 或 `ci-results/rpf32/`；历史 reviewed bytes 与 LocalFile corpus 不刷新、不迁移，不引入自动 fallback、delete、versioning/Object Lock、2PC、HA/replication、lifecycle/GC、真实云账号、Web upload、Production deploy/release。SeaweedFS 证据是单节点 disposable compatibility proof，不等于 Production durability/HA。
- RPF-33 容量与 Large-Trace investigation：`mvn -q test package -f control-plane/pom.xml`、`mvn -q package -f spikes/rpf-33/java/pom.xml`、`python spikes/rpf-33/probe.py --run --output-dir .local/rpf-33/<run>`、`python spikes/rpf-33/verify-evidence.py --result <rpf33-result.json>` 与 `.github/workflows/rpf-33-capacity-spike.yml` 的 hosted Medium proof 覆盖真实 disposable PostgreSQL/HTTP JSON/durable worker/SeaweedFS/OTel 资源、Small/Medium/Large fixture、API payload/SQL log/EXPLAIN/candidate A-B、worker 1/2/4、claim/reclaim、S3 size/orphan、OTel isolation、资源与 exact cleanup。`--hold-web` 仅用于本机人工 desktop observation；不等于 real-device evidence。输出仅在忽略的 `.local/rpf-33/` 或 `ci-results/rpf33/`；不做正式 schema/index migration、pagination/virtualization/streaming implementation、broker/scheduler/GC/Production/release/deploy，不刷新 reviewed corpus。
- Python runtime 当前无独立 lint/build 命令；文档/Project State 仍按语义/链接、Git diff/history/status 和 secret/ignore 边界验证。
- 真实产品命令引入后，按脚本实际语义更新本节，注明 build 已包含的检查以避免重复；不创建空洞 CI。
- 当前 runtime/CI/deployment/version 状态仅见 CURRENT_STATE。

## 4. Git / delivery / release model

- 默认政策：`push-only`（commit → push）；实际 upstream 与交付结果见 CURRENT_STATE。新增可信 deployment workflow/endpoint 时通过后续 Plan 更新模型及授权。
- 对已授权的实施任务，coherent change 在必要验证后默认 focused commit；实现、必要验证和适用 Project State 更新完成后，默认提交剩余改动，并普通 push 到已存在且身份明确的既定 upstream，不逐次请求确认。
- 审查、解释、诊断类请求本身不授权 commit/push 或其他写操作；当前明确任务限制优先。
- remote 尚未建立时，不猜测 owner/name/visibility。仅在身份、可用认证与创建/连接授权明确后操作；不擅自改变仓库身份或可见性。
- 禁止 force push、擅自 rebase/history rewrite；不得 amend/rewrite `efb0101` 掩盖 bootstrap 转换错误。
- 已 commit 不等于已交付；push、CI、deployment 的成功、失败或未执行分别如实报告。部署授权不得从普通 push 授权推导。
- deploy/release/publish 仅在仓库规则或当前明确授权下执行；RPF-00 不授权这些操作。未来 push 会触发部署时须先核对对应 workflow 与授权。
- 产品版本、CHANGELOG、tag 与 release 验证按真实发布流程定义，不把 bootstrap commit 当产品版本。

## 5. Product checks and skill routing

- 关键 Reliability 不变量以 PROJECT_BRIEF 为准，特别是状态有效性、证据身份/覆盖率、UNKNOWN_OUTCOME reconcile、Regression 质量门槛与不可静默改写历史。
- Desktop Web 是 portfolio-critical Primary surface。相关 UI Plan 必须把 canonical UX 合同纳入正式验收，不延期为末期美化。
- 普通工程执行：`frugal-dev-runner`；Web UI/浏览器体验：`ui-real-device-qa`。技能提供通用执行方法，不扩大当前任务权限。
- 正式 Plan/Handoff 和唯一主结果 `TASK_RESULT` 默认中文；保留 Plan ID、`Status: Complete / Partial / Blocked`。Optional USER CHECK 仅为非阻塞建议，不替代正式 Acceptance。

## 6. Stop conditions

缺少必要产品决定、远端身份/访问条件、操作权限，或下一步需突破产品/任务安全边界时，完成可独立进行的本地工作后报告具体缺口；不得猜测授权或凭据。技术未知本身应先在当前任务范围内调查。
